language: python
python:
  - "2.7"

sudo: false

env:
  - PLATFORMIO_CI_SRC=Main/Main.ino

install:
  - pip install  -U platformio

notifications:
  slack:
    on_success: always
    on_failure: always
